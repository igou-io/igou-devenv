# Bundle: every read-only identity an inspection session may need. Stacks the
# included profiles (kubeconfigs merge into one KUBECONFIG list with contexts
# named after the profile; first one is current). Add truenas-ro once a
# READONLY_ADMIN API key exists.
INCLUDE=ocp-cluster-reader,rk8s-cluster-reader,routeros-ro
PERMISSIONS=readonly
