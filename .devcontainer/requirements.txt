# Managed by Renovate — pin versions for automated updates
ansible-core==2.21.3
ansible-navigator==26.8.0
ansible-builder==3.1.1
ansible-rulebook==1.3.1
ansible-runner==2.4.3
ansible-lint==26.8.0
ansible-creator==26.8.0
molecule==26.8.0
awxkit==24.6.1
yq==4.1.2
kubernetes==36.0.3
jmespath==1.1.0
lxml==6.1.2
paramiko==5.0.0
scp==0.16.1
# RouterOS API client for the read-only routeros-ro profile (community.routeros.api / direct use)
librouteros==4.2.0
# TrueNAS JSON-RPC WebSocket client for the read-only truenas-ro profile.
# Held at 15.0.x: ansible-rulebook pins websockets<15.1 (renovate.json allowedVersions
# rule mirrors this cap — lift both when rulebook does).
websockets==15.0.1
